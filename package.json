{
  "name": "fsda-project-workspace",
  "private": true,
  "scripts": {
    "dev": "powershell -ExecutionPolicy Bypass -File scripts/start-dev.ps1",
    "dev:frontend": "npm --prefix frontend run dev:frontend",
    "dev:backend": "mvn spring-boot:run --file backend/pom.xml",
    "dev:auth": "npm --prefix server run dev",
    "build": "npm --prefix frontend run build",
    "build:frontend": "npm --prefix frontend run build",
    "lint:frontend": "npm --prefix frontend run lint"
  }
}
